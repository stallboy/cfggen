// 跨模块引用测试用例
// 用于测试 T024: 跨模块引用跳转到定义

// 当前模块：task
// 跨模块引用格式：module.table

table TaskConfig[config_id] {
    name: str;
    // 跨模块引用格式
    item_id: int ->other.item;
    monster_id: int ->monster.monster;
    // 测试复杂的跨模块引用
    rewards: list<int> =>item.reward;
}
