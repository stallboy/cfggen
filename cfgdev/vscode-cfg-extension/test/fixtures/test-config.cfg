
struct User {
    id: int;
    name: str;
    email: str;
}

table Role[roleId] {
    roleId: int;
    roleName: str;
    userId: int -> User[id];  // 外键引用到User
}

table Permission[permId] {
    permId: int;
    permName: str;
    roleIds: list<int> => Role[roleId];  // 列表外键引用
}
