struct Position {
    x: int;
    y: int;
}

table Task[id] {
    id: int;
    name: str;
}

interface IMovable {
    struct Walk {
        speed: int;
    }
}

struct Test {
    pos: P  // 测试补全 Position
    task: T  // 测试补全 Task
}