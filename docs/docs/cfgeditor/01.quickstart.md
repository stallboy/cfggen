---
layout: page
title: 快速开始
parent: 编辑器
nav_order: 1
---

# 快速开始

## 🚀 启动server

使用cfgeditor.exe时，要先开启server

启动脚本如下：

```bash
java -jar ../cfggen.jar -datadir ../example/config -gen server
```

服务器启动时可以配置以下选项：

- `port=3456` - 为cfgeditor.exe提供服务的端口
- `note=_note.csv` - 非json记录的标注存储位置
- `watch=0` - x>0表示数据文件修改x秒后自动重载配置
- `postrun=null` - xx.bat或xx.sh，用于重载配置后的额外动作

---

## 页面选择

- 默认是记录，可以通过以下按钮来切换展示表（schema）信息还是展示记录（data）信息

![page choose](pageChoose.png)

- 在主界面中右键，则可以切换到编辑状态

![edit menu](editmenu.png)


## 导航

- 最主要的导航是左上角的table和id选择框

    ![nav](nav.png)


- 关联数据：是当前记录相关联的记录列表
    ![finder](finder.png)
- 访问历史
- 修改历史
- 搜索
    ![search](search.png)

- 关系图导航

    - 先设置关系图，如下图
        1. 先在右视图中看记录关系
        2. 然后点击`固定当前页面`
        3. 可以修改目录名称，然后点击`设置固定页面`
    ![nav by ref setting](navrefsetting.png)

    - 使用关系图，如下图，双击左边记录关系图中的记录，会在右边打开相应记录，方便编辑
    ![nav ref](navref.png)

## 新增或更新记录

- 如下图更改后，点击更新，如果id已存在，则是更新，否则则是新增
![addOrUpdate](addOrUpdate.png)

- 如果table下还不存在记录，id搜索框会有+new按钮，使用它来新增记录
![add](add.png)

- 在设置-+ 里直接添加json
![addJson](addJson.png)

- ai生成

## 删除记录

在设置-操作里点击`删除当前数据`
![deleteRecord](deleteRecord.png)